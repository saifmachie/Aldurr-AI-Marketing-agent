# Plan history

The strategist agent reads the last 4 files here (by filename, sorted) to
avoid repeating concepts. Empty for now — this is a cold start.

Once you approve a week (per the approval loop in
`docs/AlDurrAgentArchitecture.md` §6), copy that week's final
`output/week-<id>-plan.json` into this folder so future strategist runs see
it. This is manual in Phase 2 — the orchestrator automates it in Phase 3.
