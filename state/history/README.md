# Concept history

The strategist reads the last 4 `quarter-*.json` files here (by filename,
sorted) to avoid repeating concepts across quarters. Empty for now — this
is a cold start.

Written automatically by `node index.js approve-batch <quarter>` once a
quarter's full batch is approved — no manual copying needed.
