# App screenshots

Real captures only — per the architecture spec, no image model renders
Arabic text, and screenshots are the one design element that isn't produced
by Canva. Drop real `.png` captures here and reference the filename in a
post's `design_brief.screenshot` field for the designer agent to pick up.

Empty for now — no real screenshots have been provided yet.
